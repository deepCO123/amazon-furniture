# 🛠️ دليل الأدوات والتقنيات المستخدمة في المشروع (Tech Stack & Tools)

دليل شامل وشرح تفصيلي لكافة الأدوات، والمكتبات، وطبقات الأمان والأداء المستخدمة في مشروعي **الباك إند (Backend)** و **الفرونت إند (Frontend)** لمتجر **Amazon Furniture**.

---

## 🏛️ 1. نظرة عامة على معمارية المشروع (System Architecture)

المشروع مبني بمعمارية حديثة **Full-Stack Hybrid** تجمع بين:
- **Next.js 16 (App Router)** للواجهة الأمامية وتوليد الصفحات والـ SEO.
- **Express.js API Server** كخادم خلفي متخصص في الأمان والمصادقة الصارمة وخدمات الـ SMTP وإدارة الكاش.
- **In-Memory Caching JSON Database** لتخزين البيانات بسرعة فائقة (Zero Disk I/O) داخل `src/data/*.json`.

```
[ Frontend: Next.js 16 (React 19 / TypeScript) ] : Port 3000
       │
       ▼ (Proxy Rewrites: /api/auth/* & /api/cache-stats)
[ Backend: Express.js API Server (Port 5000) ]
       │
       ├── Security: Helmet + CORS + RateLimit + Bcrypt + JWT (HttpOnly)
       ├── Performance: Gzip Compression + In-Memory RAM Cache
       └── Data: Write-Through Persistence to src/data/*.json
```

---

## ⚙️ 2. أدوات ومكتبات الباك إند (Backend Tech Stack)

| الأداة / المكتبة | الإصدار | الوظيفة ودورها في المشروع |
|:---|:---|:---|
| **Node.js** | `v20+` / `v24` | بيئة تشغيل جافاسكربت الأساسية على السيرفر. |
| **Express.js** | `^4.19.2` | إطار العمل الأساسي لبناء خادم الـ RESTful API، وتنظيم المسارات والميدلوير. |
| **Helmet** | `^7.1.0` | **أمان:** تأمين ترويسات HTTP (Headers)، ومنع ثغرات Clickjacking عبر `X-Frame-Options: DENY`، وتفعيل `X-Content-Type-Options: nosniff`، و `HSTS` (Strict-Transport-Security)، وسياسة أمان المحتوى (CSP). |
| **CORS** | `^2.8.5` | **أمان:** التحكم في مشاركة الموارد عبر النطاقات؛ تم ضبطه بدقة ليسمح فقط لنطاق الفرونت إند المعتمد (`CLIENT_URL` أو `localhost:3000`) مع تفعيل `credentials: true` للسماح بتبادل الكوكيز الآمنة. |
| **express-rate-limit** | `^7.3.1` | **أمان:** حماية السيرفر من هجمات حجب الخدمة (DoS) ومنع محاولات التخمين العشوائي لكلمات المرور (Brute-Force) عبر وضع حد أقصى للمحاولات خلال 15 دقيقة على مسارات الدخول `/api/auth/*`. |
| **cookie-parser** | `^1.4.6` | **أمان:** قراءة وفك تشفير الـ Cookies القادمة في الطلبات للوصول إلى `token` المحفوظ في كوكي مشفرة `HttpOnly` لا يمكن سرقتها عبر JavaScript. |
| **jsonwebtoken (JWT)** | `^9.0.2` | **مصادقة:** توليد وتوقيع توكنات الجلسات المشفرة مع تحديد مدة الصلاحية وتضمين بيانات المستخدم وصلاحيته (Role). |
| **bcryptjs** | `^2.4.3` | **أمان وتشفير:** تشفير كلمات مرور المستخدمين والإدارة بخوارزمية التجزئة أحادية الاتجاه (Hashing) مع 12 جولة Salt، لحمايتها ضد هجمات القواميس والتسريبات. |
| **compression** | `^1.7.5` | **أداء وسرعة:** ضغط استجابات الـ JSON الصادرة من السيرفر باستخدام خوارزمية **Gzip** للردود أكبر من 1KB لتقليل استهلاك الباندويث وتسريع وصول البيانات للعميل. |
| **Nodemailer** | `^6.9.14` | **خدمات:** إرسال إيميلات تلقائية واحترافية عبر بروتوكول SMTP (رسائل الترحيب للعملاء الجدد، وفواتير تأكيد الطلبات، وإشعارات الإدارة الفورية). |
| **Mongoose (MongoDB)** | `^9.10.1` | **قاعدة البيانات:** إدارة الاتصال بقاعدة بيانات MongoDB السحابية أو المحلية، وتطبيق الـ Schemas الصارمة والـ Validation وربط العلاقات المتبادلة عبر `ObjectId`. |
| **In-Memory RAM Cache**<br>*(Hybrid Engine)* | `v2.0` | **أداء فائق:** محرك كاش هجين (`services/jsonCache.js`) يقوم بملء الذاكرة العشوائية (RAM) من MongoDB لخدمة طلبات `GET` في **0 مللي ثانية**، مع التحديث الفوري المزدوج في MongoDB وملفات النسخ الاحتياطي عند حدوث (`POST`, `PUT`, `DELETE`). |

---

## 🎨 3. أدوات ومكتبات الفرونت إند (Frontend Tech Stack)

| الأداة / المكتبة | الإصدار | الوظيفة ودورها في المشروع |
|:---|:---|:---|
| **Next.js** | `16.2.6` | إطار العمل الكامل (App Router) لتوليد الصفحات المجمعة مسبقاً (SSG)، ومسارات الـ API Routes، ومحرك التجميع الخارق **Turbopack**. |
| **React** & **React DOM** | `19.2.4` | أحدث إصدار من مكتبة بناء واجهات المستخدم التفاعلية من فيسبوك. |
| **TypeScript** | `^5` | لغة البرمجة التي تضيف الكتابة الصارمة (Static Types) لمنع الأخطاء أثناء التطوير وتحسين جودة الكود. |
| **TailwindCSS** | `^4` | أحدث إصدار من محرك التصميم وتنسيق الواجهات السريعة والمرنة بدون كتابة CSS تقليدي مكرر. |
| **Zustand** | `^5.0.14` | إدارة الحالة العامة للتطبيق (State Management) خفيفة جداً وسريعة؛ مسؤولة عن: سلة المشتريات (`cartStore`)، قائمة المفضلة (`wishlistStore`)، والمنتجات (`productStore`). |
| **Framer Motion** | `^12.40.0` | مكتبة التحريك السينمائي والأنيميشن الاحترافي، حركات الانتقال وسلايدر الهيرو والعناصر التفاعلية. |
| **Anime.js** | `^4.4.1` | مكتبة تحريك متقدمة لدعم التأثيرات المعقدة والحركية الدقيقة. |
| **Lucide React** | `^1.17.0` | حزمة أيقونات SVG عصرية وخفيفة تمثل كل أيقونات المتجر ولوحة التحكم. |
| **React Hook Form** | `^7.76.1` | إدارة نماذج الإدخال بكفاءة عالية وأداء ممتاز يمنع إعادة تصيير الصفحة غير الضرورية (Re-renders). |
| **Zod** & **@hookform/resolvers** | `^4.4.3` | التحقق من صحة المدخلات والبيانات (Schema Validation) في النماذج قبل إرسالها للباك إند. |
| **Jose** | `^6.2.12` | مكتبة تشفير وتحقق من توكنات JWT متوافقة مع الـ Edge Runtime داخل مسارات Next.js API Routes. |
| **clsx** & **tailwind-merge** | أحدث إصدار | دمج كلاسات Tailwind الشرطية وتفادي التعارض بين التنسيقات بسهولة. |

---

## 💾 4. طبقة قاعدة البيانات والتخزين (Database & Storage Architecture)

بدلاً من الاعتماد على خادم قاعدة بيانات خارجي يستهلك موارد ثقيلة، يعتمد المشروع على **قاعدة بيانات JSON مهيكلة ومحمية محلياً** داخل مجلد `src/data/`:

1. `products.json`: كتالوج المنتجات الكامل (الأسعار، الأبعاد، الأخشاب، الصور، المخزون).
2. `orders.json`: سجل طلبات الشراء وبيانات شحن العملاء وتفاصيل الفواتير.
3. `customers.json`: سجل العملاء المسجلين، وطريقة التسجيل، وإجمالي المشتريات.
4. `quotations.json`: عروض الأسعار الرسمية للشركات والعملاء الكبار.
5. `returns.json`: طلبات الاستبدال والاسترجاع وحالة المعاينة والمبالغ المستردة.
6. `consultations.json`: طلبات المعاينة والاستشارة المجانية داخل المنصورة وأونلاين.
7. `emails.json`: الأرشيف الكامل لكافة الإيميلات المرسلة مع نصوص HTML.

> 🚀 **سر الأداء العالي:** يتم قراءة هذه الملفات عبر محرك **In-Memory Cache** تم برمجته داخل الباك إند والفرونت إند، مما يلغي تماماً بطء القراءة من الهارد ديسك ويضمن سرعة استجابة فورية (0ms) مع استمرار الحفظ التلقائي للبيانات على القرص.

---

## 🚦 5. كيفية تشغيل المشروع وتزامن الأدوات

في المجلد الرئيسي للمشروع، تم ضبط أداة:
- **`concurrently`**: تقوم بتشغيل خادم **Next.js (Port 3000)** وخادم **Express.js (Port 5000)** معاً في نفس الوقت عبر سطر أوامر واحد:
  ```bash
  npm run dev
  ```
  أو تشغيل الباك إند منفرداً عبر:
  ```bash
  npm run backend
  ```
